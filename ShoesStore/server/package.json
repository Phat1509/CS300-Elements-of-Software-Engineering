{
    "name": "app",
    "version": "1.0.50",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "bun run --watch src/index.ts",
        "gen-db-types": "kysely-codegen --out-file src/db/types.ts --dialect postgres"
    },
    "dependencies": {
        "@elysiajs/jwt": "^1.4.0",
        "elysia": "latest",
        "kysely": "^0.28.8",
        "kysely-bun-sql": "^0.1.0"
    },
    "devDependencies": {
        "bun-types": "latest",
        "kysely-codegen": "^0.19.0",
        "kysely-ctl": "^0.19.0"
    },
    "module": "src/index.js"
}
